local L = BigWigs:NewBossLocale("Watchkeeper Gargolmar", "koKR")
if not L then return end
if L then
	L.hellfire_watcher = "지옥불 감시병"
end

L = BigWigs:NewBossLocale("Vazruden", "koKR")
if L then
	L.nazan = "나잔"
end
