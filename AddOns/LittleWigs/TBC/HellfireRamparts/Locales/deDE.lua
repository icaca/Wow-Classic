local L = BigWigs:NewBossLocale("Watchkeeper Gargolmar", "deDE")
if not L then return end
if L then
	L.hellfire_watcher = "Höllenfeuerwächter"
end

L = BigWigs:NewBossLocale("Vazruden", "deDE")
if L then
	L.nazan = "Nazan"
end
